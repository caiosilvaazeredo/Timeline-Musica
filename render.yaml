services:
  - type: web
    name: vitrola
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: BASE_URL
        sync: false        # ex: https://vitrola.onrender.com
      - key: JOIN_URL
        sync: false        # opcional: dominio curto para jogadores, ex https://vitrola.page
      - key: SPOTIFY_CLIENT_ID
        sync: false
      - key: SPOTIFY_CLIENT_SECRET
        sync: false
      - key: YT_API_KEY
        sync: false
